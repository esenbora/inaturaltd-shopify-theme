name: Blog Cron

on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    defaults:
      run:
        working-directory: blog-engine

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: blog-engine/package-lock.json

      - name: Restore processed ledger
        uses: actions/cache/restore@v4
        with:
          path: blog-engine/processed.json
          key: blog-processed-${{ github.run_id }}
          restore-keys: |
            blog-processed-

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Generate blog article drafts
        run: npm start
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPENROUTER_MODEL: ${{ secrets.OPENROUTER_MODEL }}
          SHOPIFY_SHOP: ${{ secrets.SHOPIFY_SHOP }}
          # Shopify auth: a static token OR client_credentials (client_id + secret).
          # client_credentials tokens are ~24h, so the cron mints a fresh one each run.
          SHOPIFY_ADMIN_TOKEN: ${{ secrets.SHOPIFY_ADMIN_TOKEN }}
          SHOPIFY_CLIENT_ID: ${{ secrets.SHOPIFY_CLIENT_ID }}
          SHOPIFY_CLIENT_SECRET: ${{ secrets.SHOPIFY_CLIENT_SECRET }}
          SHOPIFY_BLOG_HANDLE: ${{ secrets.SHOPIFY_BLOG_HANDLE }}
          # Draft-first: leave AUTO_PUBLISH unset so articles land hidden for editorial review.
          # Set AUTO_PUBLISH=true only to publish live automatically.
          BATCH: ${{ vars.BATCH || '2' }}

      - name: Ensure ledger exists
        if: always()
        run: test -f processed.json || printf '{"version":1,"posts":{}}\n' > processed.json

      - name: Save processed ledger
        if: always()
        uses: actions/cache/save@v4
        with:
          path: blog-engine/processed.json
          key: blog-processed-${{ github.run_id }}
